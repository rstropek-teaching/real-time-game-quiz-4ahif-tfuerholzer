{
  "name": "MobileComputingMultiplayer",
  "version": "0.0.1",
  "dependencies": {
    "express": "^4.16.2",
    "socket.io": "^2.0.4",
    "moment": "^2.20.1"
  },
  "devDependencies": {
    "@types/express": "^4.11.0",
    "@types/node": "^8.5.2",
    "@types/socket.io": "^1.4.31",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "typescript": "^2.6.2"
  },
  "scripts": {
    "build": "tsc --outDir ./dist && cd dist && copy login.js ./public && cd ..",
    "start-server": "node ./dist/server.js"
  }
}
